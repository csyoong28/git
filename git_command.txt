cd C:\Users\soonyoong.chia\github\git
#to initialize a repository
git init

#to check states
git status

#to add into staging area
git add index.html
git add git_command.txt

#to commit, need to provide message
Git commit -m "My first commit"

#to commit, if want to put more text, will use default text editor like notepad++
Git commit